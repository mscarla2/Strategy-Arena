# Side-by-Side Body Intraday Backtest
# Install with: pip install -r side_by_side_backtest/requirements.txt

# ── Core data stack ──────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=14.0.0          # Parquet disk cache for OHLCV bars

# ── Pydantic models ───────────────────────────────────────────────────────────
pydantic>=1.10,<3.0      # v1 or v2 both supported (validator syntax is v1-style)

# ── Data providers ────────────────────────────────────────────────────────────
yfinance>=0.2.38         # Primary free OHLCV source (no API key needed)
requests-cache>=1.0.0    # HTTP-level cache to reduce Yahoo rate-limiting (optional but recommended)
# alpaca-py>=0.20.0      # Uncomment if using Alpaca as provider

# ── Auto-tuning ───────────────────────────────────────────────────────────────
optuna>=3.0.0            # Bayesian parameter optimisation (TPE sampler)

# ── Visualisation (optional but recommended for heatmaps / equity curve) ──────
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Console output ────────────────────────────────────────────────────────────
rich>=13.0.0             # Pretty tables in the terminal (graceful fallback if absent)

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=8.0.0
